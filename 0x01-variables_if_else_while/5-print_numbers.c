#include <stdio.h>

/**
 * main - Entry point
 * Return: 0 (success)
 */
int main(void)
{
	int num;
	 /* print single base 10 numbers */
	for (num = 0; num < 10; num++)
		printf("%d", num);
	return (0);
}
